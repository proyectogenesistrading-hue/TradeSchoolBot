# TradeSchoolBot


1. Crear el entorno virtual:

python3 -m venv venv

2. Activarlo:

source venv/bin/activate

3. Instalar las dependencias (ahora sí sin error):

pip install -r requirements.txt

4. Correr el bot:

python bot.py